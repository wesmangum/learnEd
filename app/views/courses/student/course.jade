extends ../../shared/template

block content
  h1(data-id= course._id)= course.title
  h3= course.description


    //   CHANGE THIS INTO STUDENT VIEW


  #flashcards
    if(course.hasFlashCards)
      form(method='get', action='/courses/#{course._id}/flashcards')
        button.button.showFlash Show Flashcards



  #tests
    if(course.hasTest)
      form(method='get', action='/courses/#{course._id}/tests')
        button.button.showTest Take Test





  //-
  //- #flashContainer.well.well-sm
  //-   #flashForm
  //-   #flashcards
  //-   button#addFlashCard New Flash Card
  //-
  //- #testContainer.well.well-sm
  //-   #testForm
  //-   #test
  //-   button#addTest Add Test
  //-

block scripts
  script(src='/js/es6/compiled/courses.js')
